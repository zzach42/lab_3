#include "human.h"

Human::Human(std::string name, int height)
    : name(name), height(height) {
}

std::string Human::toString() const {
    return name + ", рост: " + std::to_string(height);
}

void Human::print() const {
    std::cout << toString() << std::endl;
}